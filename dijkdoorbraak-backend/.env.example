# PostgreSQL connection string
DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/dijkdoorbraak

# Admin login password (checked server-side — never expose to client)
ADMIN_PASSWORD=change-me

# Secret used to sign the admin session token (use a long random string)
ADMIN_TOKEN_SECRET=change-me-use-openssl-rand-hex-32
